/**
 *  JavaScript Comparison Operators
 *  author: Joshuép Jr.
 */


/**
 * A comparison operator returns a boolean value
 * indicanding that the conparison is true or false
*/
 
// ============ Equal (==) Operator ============ 
// Returns true if the operands are equal
 let age = 23;
 let number = 23;
 let equality = age == number;
 console.log("Equality: ", equality);

// ============ Not Equal Operator ============ 
age = 24;
equality = age != number;
console.log("Not Equality: " + equality);
// ============ Strict Equal Operator ============ 

// ============ Strict Not Equal Operator ============ 

// ============ Greater than Operator ============ 

// ============ Greater than or equal to ============ 

// ============ Less than ============ 

// ============ Less than or equal to ============ 
let result1 = 30 > 34;
console.log(result1); 
// false, because 30 is less than 34

let result2 = 30 < 23;
console.log(result2);
// false, because 30 is grater than 23

